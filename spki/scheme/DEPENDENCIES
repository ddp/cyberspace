# Cyberspace Dependencies
# Host-specific requirements for building and running
#
# This file is checked per-host. Add platform-specific notes.

## Required

chicken-scheme    # Chicken Scheme 5.x compiler and runtime
                  # brew install chicken (macOS)
                  # apt install chicken-bin (Debian/Ubuntu)

libsodium         # NaCl-compatible crypto library
                  # brew install libsodium (macOS)
                  # apt install libsodium-dev (Debian/Ubuntu)

## Chicken Eggs (install with: chicken-install <egg>)

srfi-1            # List library
srfi-13           # String library
srfi-18           # Multithreading
srfi-69           # Hash tables
matchable         # Pattern matching
json              # JSON parsing

## Optional

wget              # For downloading documentation
                  # brew install wget

## Host-Specific Notes

# lambda (Intel x86_64):
#   libsodium at /usr/local/lib
#   SODIUM_FLAGS="-I/usr/local/include -L/usr/local/lib -L -lsodium"

# fulcrum (Apple Silicon ARM64):
#   libsodium at /opt/homebrew/lib
#   SODIUM_FLAGS="-I/opt/homebrew/include -L/opt/homebrew/lib -L -lsodium"

## Verification

# Check Chicken:
#   csc -version

# Check libsodium:
#   pkg-config --modversion libsodium
#   # or: ls /usr/local/lib/libsodium* /opt/homebrew/lib/libsodium*

# Check eggs:
#   chicken-status

# Cyberspace Scheme - Required Eggs
# Install: chicken-install $(cat eggs.txt | grep -v '^#')

# Core SRFIs
srfi-1      # list utilities (filter, fold, etc.)
srfi-4      # homogeneous numeric vectors (u8vector)
srfi-13     # string utilities
srfi-14     # character sets
srfi-18     # multithreading (mdns, enroll, gossip)
srfi-69     # hash tables

# Optional (for development)
# apropos   # symbol search

;;;; cert.import.scm - GENERATED BY CHICKEN 5.4.0 -*- Scheme -*-

(##sys#with-environment
  (lambda ()
    (##sys#register-compiled-module
      'cert
      'cert
      (scheme#list)
      '((make-key-principal . cert#make-key-principal)
        (make-keyhash-principal . cert#make-keyhash-principal)
        (key-principal? . cert#key-principal?)
        (keyhash-principal? . cert#keyhash-principal?)
        (principal-public-key . cert#principal-public-key)
        (principal-hash-alg . cert#principal-hash-alg)
        (principal-hash . cert#principal-hash)
        (make-tag . cert#make-tag)
        (make-all-perms . cert#make-all-perms)
        (tag? . cert#tag?)
        (all-perms? . cert#all-perms?)
        (tag-sexp . cert#tag-sexp)
        (make-validity . cert#make-validity)
        (validity? . cert#validity?)
        (validity-not-before . cert#validity-not-before)
        (validity-not-after . cert#validity-not-after)
        (make-cert . cert#make-cert)
        (cert? . cert#cert?)
        (cert-issuer . cert#cert-issuer)
        (cert-subject . cert#cert-subject)
        (cert-tag . cert#cert-tag)
        (cert-validity . cert#cert-validity)
        (cert-propagate . cert#cert-propagate)
        (make-signed-cert . cert#make-signed-cert)
        (signed-cert? . cert#signed-cert?)
        (signed-cert-cert . cert#signed-cert-cert)
        (signed-cert-signature . cert#signed-cert-signature)
        (make-signature . cert#make-signature)
        (signature? . cert#signature?)
        (signature-hash-alg . cert#signature-hash-alg)
        (signature-cert-hash . cert#signature-cert-hash)
        (signature-sig-bytes . cert#signature-sig-bytes)
        (hash-alg->string . cert#hash-alg->string)
        (create-cert . cert#create-cert)
        (sign-cert . cert#sign-cert)
        (verify-signed-cert . cert#verify-signed-cert)
        (verify-chain . cert#verify-chain)
        (tag-implies . cert#tag-implies)
        (principal->sexp . cert#principal->sexp)
        (sexp->principal . cert#sexp->principal)
        (tag->sexp . cert#tag->sexp)
        (sexp->tag . cert#sexp->tag)
        (validity->sexp . cert#validity->sexp)
        (sexp->validity . cert#sexp->validity)
        (cert->sexp . cert#cert->sexp)
        (sexp->cert . cert#sexp->cert)
        (signature->sexp . cert#signature->sexp)
        (signed-cert->sexp . cert#signed-cert->sexp)
        (sexp->signed-cert . cert#sexp->signed-cert)
        (signed-cert->string . cert#signed-cert->string)
        (signed-cert-of-string . cert#signed-cert-of-string))
      (scheme#list)
      (scheme#list)
      (scheme#list))))

;; END OF FILE
